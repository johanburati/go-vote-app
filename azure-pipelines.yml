trigger:
- master

pool:
  vmImage: 'Ubuntu-16.04'

variables:
  imageName: 'go-vote-app:$(build.buildId)'
 
steps:
- script: docker build --no-color -f Dockerfile -t $(ImageName) .
  displayName: 'docker build'
